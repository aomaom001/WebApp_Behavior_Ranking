# Dependencies
node_modules/

# Python (data pipeline / API)
__pycache__/
*.pyc
.venv/
venv/

# Secrets — DB credentials for the API live here, never commit
.env

# Source data exports (large Excel files live outside the repo)
*.xlsx
*.xls
*.csv

# Pipeline backups (note: data/detail/*.json IS tracked — needed for drill-down on Pages)
data/data.backup-*.json

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Build output (none today — reserved if a build step is added later)
dist/
